#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Temporary disable lint-staged for Windows compatibility
echo "⚠️  Lint-staged temporarily disabled for Windows compatibility"
echo "   Run 'npm run lint-staged' manually if needed"
exit 0
